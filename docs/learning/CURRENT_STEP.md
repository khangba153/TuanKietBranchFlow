# Điểm dừng học tập BranchFlow

Cập nhật: 2026-09-19. Đây là ghi chú tiếp nối, không thay thế việc đọc code mới nhất.

## Chủ đề hiện tại

Người học báo đã smoke test Web chức năng báo sai đúng dự đoán: form chỉ hiện cho `COMPLETED`, binding/bộ đếm/loading hoạt động, thành công chuyển ngay sang `NEEDS_REVIEW` với lý do/thời điểm Backend trả về, F5 giữ đúng dữ liệu, lỗi kết nối giữ nội dung và mở lại nút, giao diện mobile không tràn. Đây là kiểm thử runtime thủ công, chưa phải automated UI test. Vertical slice B2 gọi món EMPLOYEE hiện đã hoàn thành ở phạm vi MVP: đọc menu → cấu hình món/giỏ → tạo đơn → danh sách/chi tiết → báo sai, cùng unit test Service và kiểm chứng Swagger/SQL. Bước hiện tại là full build/test, rà soát diff/secret rồi commit và phát hành lại bản public trước khi chuyển B3 Menu ADMIN.

Người học báo đã kiểm tra `PATCH /api/orders/{orderId}/report` bằng Swagger và SQL local, kết quả đúng dự đoán: nhánh thành công cập nhật `NEEDS_REVIEW` cùng lý do/người báo/thời điểm và lần báo lại bị từ chối, không ghi đè dữ liệu đầu tiên. Đây là kiểm thử thủ công qua API/SQL thật, chưa phải integration test tự động. Backend báo sai được chốt ở mức hiện tại. Bước hiện tại chuyển sang nối Web trong `MyOrderDetail.razor`, vẫn theo ba vòng: markup Bootstrap tĩnh → state/method và Web API service → gắn điều kiện/binding/event rồi smoke test.

Ngày 2026-09-22, backend chức năng employee báo sai đơn đã hoàn thành code: `ReportOrderRequestDTO`, `ReportOrderResponseDTO`, `ReportOrderResult`, Repository lấy entity có tracking theo đồng thời order ID/employee ID, `OrderService.ReportOrderAsync` chuẩn hóa và kiểm tra lý do, chỉ cho `COMPLETED → NEEDS_REVIEW`, lưu người báo/thời điểm, và `PATCH /api/orders/{orderId}/report` ánh xạ 200/400/401/403/404/409/500. API build Release đạt 0 warning, 0 error. Hai unit test mới chứng minh đơn đã `NEEDS_REVIEW` không bị ghi đè và đơn `COMPLETED` được cập nhật/lưu đúng mà không mở transaction thủ công; agent chạy riêng 2/2 pass và toàn bộ suite 19/19 pass. Đây vẫn là test Service bằng Moq; bước hiện tại là kiểm chứng endpoint qua Swagger với SQL local, sau đó mới nối Web vào trang chi tiết đơn.

Ngày 2026-09-22, màn Web `/my-orders/{OrderId:int}` đã hoàn thành đủ ba vòng Razor. Trang gọi `OrderApiService.GetMyOrderDetailAsync`, phân biệt loading/error/no-data/thành công, hiển thị snapshot món/size/topping/ghi chú, tổng tiền và trạng thái; danh sách `/my-orders` đã truyền đúng `order.Id` qua liên kết **Xem chi tiết**. Agent kiểm tra Web Release đạt 0 warning, 0 error và không có trailing whitespace; người học báo đã test runtime đúng dự đoán, gồm điều hướng, dữ liệu thật, trường hợp không có topping/ghi chú, ID không tồn tại, đơn của employee khác, retry, quay lại và responsive. Đây là smoke test thủ công, chưa phải automated UI test. Phần còn lại cuối của mốc B2 là **employee báo sai đơn hàng**: thiết kế endpoint đổi đơn thuộc chính employee từ `COMPLETED` sang `NEEDS_REVIEW`, lưu lý do/người báo/thời điểm; sau đó nối thao tác vào trang chi tiết. Chưa bắt đầu code endpoint.

Ngày 2026-09-20, ba request DTO, `CreateOrderResponseDTO` và `CreateOrderResultDTO` của `POST /api/orders` đã được tạo. Result phân biệt branch không tồn tại, thiếu phân công, request nghiệp vụ không hợp lệ và nhánh thành công. `IOrderRepository`/`OrderRepository` đã đọc hàng loạt ProductSize, Topping và NoteOption hợp lệ, gồm navigation cần cho snapshot và quy tắc topping; repository kế thừa `AddAsync(SalesOrder)` để chuẩn bị lưu graph đơn và đã được đăng ký Scoped trong API. `IOrderService`/`OrderService.CreateOrderAsync` đã kiểm tra phân công, đối chiếu các ID duy nhất với dữ liệu hợp lệ từ database, áp dụng giới hạn topping/ghi chú, tính lại giá, tạo snapshot và lưu toàn bộ graph cùng mã đơn trong một transaction. `OrderCodeResult` giữ `Code` và `SequenceNumber`; `IOrderRepository.GetNextOrderCodeAsync`/`OrderRepository` đã gọi `dbo.usp_GetNextOrderCode` bằng `DbCommand`, khai báo đủ input/output parameter và gắn transaction hiện tại của DbContext. `IUnitOfWork`/`UnitOfWork` đã có begin, commit và rollback; transaction hiện tại được giải phóng và đặt lại null sau khi kết thúc. `IOrderRepository` và `IOrderService` đều đã đăng ký Scoped. `OrdersController` đã có route `POST /api/orders`, chỉ cho role EMPLOYEE, lấy user ID từ JWT và ánh xạ result thành 201/400/403/404/500. Các unit test của `OrderService` hiện phủ những nhánh từ chối chính, hai happy path và lỗi lưu. Happy path cấu hình đã chứng minh công thức `(35.000 + 5.000 × 2 topping) × 2 món = 90.000`, đồng thời tạo đúng `OrderItemTopping` và `OrderItemNote` với ID, quantity, đơn giá và tên snapshot. Test lỗi lưu đã chứng minh khi `SaveChangesAsync()` ném exception thì Service gọi rollback đúng một lần, không commit và ném lại chính exception. Agent chạy riêng test rollback thành công, toàn bộ 17/17 test thành công và build solution Release đạt 0 warning, 0 error. Đây vẫn là Moq, chưa chứng minh thứ tự gọi tuyệt đối, stored procedure, EF Core hoặc transaction SQL thật. Chưa có test `OrdersController`. Theo quyết định tạm thời của người học, `OrderService` vẫn gộp branch không tồn tại và branch ngừng hoạt động thành `IsBranchFound = false`; đây là khoản nợ nghiệp vụ cần tách lại sau. Bước tiếp theo là test guard đơn rỗng, rồi số lượng món không dương trước khi chuyển sang test `OrdersController`.

Quy tắc phản hồi học tập được người học nhắc lại ngày 2026-09-17: trước mỗi lượt hướng dẫn phải đọc `AGENTS.md` mục **Cách hướng dẫn** và phần đầu checkpoint này. Khi người học trả lời các câu đã đặt, phải nhận xét riêng từng câu bằng nhãn **Đúng / Đúng một phần / Chưa đúng**, giải thích lý do và đưa đáp án hoàn chỉnh trước khi kiểm tra code hoặc chuyển bước; không được bỏ qua hay chỉ nhận xét chung. Khi người học gửi `check` và phần code đạt, tiếp tục luôn bước hướng dẫn kế tiếp trong cùng phản hồi, không yêu cầu gửi thêm `bắt đầu`.

Quy tắc Razor được người học chốt ngày 2026-09-20: mỗi trang/component mới phải được hướng dẫn theo ba vòng riêng — Bootstrap/HTML tĩnh trước, sau đó state và method C#, cuối cùng mới gắn dữ liệu, điều kiện và event vào markup. Không đưa một lần toàn bộ markup động và code-behind khi chưa hoàn thành vòng tĩnh.

Màn Web `/my-orders` đã hoàn thành cả ba vòng code: markup Bootstrap responsive; state/method tải danh sách, tìm theo mã, lọc hôm nay, loading/error và ánh xạ trạng thái; cuối cùng đã gắn bộ lọc, loading/error/empty và render mọi `MyOrderListItemDTO` bằng `foreach`. Người học báo đã kiểm thử runtime đúng dự đoán với danh sách thật, tìm kiếm, lọc hôm nay, kết hợp bộ lọc, trạng thái rỗng, mất/kết nối lại API và responsive; đây là smoke test thủ công, chưa phải automated UI test. Màn danh sách được chốt ở mức hiện tại. Backend `GET /api/orders/{orderId}` đã có bốn DTO chi tiết, Repository lọc đồng thời order ID và employee hiện tại rồi tải Branch/Items/Toppings/Notes, Service ánh xạ dữ liệu snapshot, và Controller trả 200/400/401/404; role khác bị `[Authorize]` chặn 403. `POST /api/orders` đã đổi sang `CreatedAtAction` để trả header `Location`. Người học báo đã test Swagger đúng dự đoán cho happy path/snapshot, ID không hợp lệ hoặc không tồn tại, đơn của employee khác cùng trả 404, thiếu token 401, ADMIN 403 và `Location` từ response 201 gọi lại được 200; đây là kiểm thử thủ công với SQL thật, chưa phải integration test tự động. Web đã có `OrderDetailApiResult` và `OrderApiService.GetMyOrderDetailAsync`, dùng `AuthorizedApiService`, đọc DTO khi thành công và giữ status/ProblemDetails khi lỗi; Web build Release đạt 0 warning, 0 error và không còn trailing whitespace. Bước hiện tại là vòng 1 của trang chi tiết: dựng Bootstrap/HTML tĩnh và kiểm tra responsive trước khi viết state/method hoặc gắn API.

Quyết định tiến độ ngày 2026-09-20: giai đoạn hiện tại chỉ viết test vừa đủ để người học hiểu cách kiểm thử và bảo vệ các nhánh nghiệp vụ rủi ro chính; ưu tiên hoàn thiện nhanh vertical slice và toàn bộ dự án. Không tiếp tục mở rộng coverage cho mọi nhánh của `EmployeeService`, `OrderMenuService`, `OrderService` hoặc Controller ở thời điểm này. Bộ 17 test hiện tại được giữ làm nền tảng; các guard test, Controller test và integration test chi tiết được ghi nhận là backlog để quay lại học kỹ sau. Người học đã gọi `POST /api/orders` bằng Swagger với JWT employee và dữ liệu thật của CN02, nhận HTTP 201; sau đó kiểm tra SQL local và báo `SalesOrder`, hai `OrderItem`, topping, ghi chú, snapshot, mã đơn và tổng 96.000 đều đúng dự kiến. Người học tiếp tục gửi `ProductSizeId` không hợp lệ, nhận HTTP 400 với `ProblemDetails` đúng và xác nhận `SalesOrder` của CN02 vẫn chỉ có một dòng. Đây là bằng chứng thủ công cho cả nhánh thành công và nhánh từ chối qua Controller → Service → Repository/UnitOfWork → EF Core/stored procedure/transaction → SQL thật. Backend `POST /api/orders` được chốt ở mức kiểm chứng hiện tại. Web đã có `OrderCreateApiResult`, `OrderApiService.CreateOrderAsync` dùng `AuthorizedApiService`, đọc response 201 hoặc `ProblemDetails`, và đăng ký Scoped trong `Program.cs`. `OrderMenu.razor` đã inject service, ánh xạ `_cartItems` thành request và có `SubmitOrderAsync`: chống gửi trùng, hiển thị trạng thái đang gửi, giữ giỏ khi lỗi, chỉ xóa giỏ/đóng modal sau response thành công và luôn mở lại trạng thái gửi trong `finally`. Agent build Web Release đạt 0 warning, 0 error. Ngày 2026-09-20, người học báo đã test thủ công runtime và kết quả đúng dự đoán: nút gửi/trạng thái loading hoạt động, lỗi được hiển thị trong modal để có thể sửa và thử lại, nhánh thành công hiển thị dữ liệu đơn do Backend trả rồi mới xóa giỏ. Đây là bằng chứng smoke test thủ công do người học báo, chưa phải automated UI test. Vertical slice tạo đơn `Web → API → Service → Repository/UnitOfWork → SQL` được chốt ở mức hiện tại. `GET /api/orders/mine` đã có `MyOrderListItemDTO`, Repository lọc theo `CreatedByUserId`, mã đơn và ngày nghiệp vụ, Service tính tổng `OrderItem.Quantity`, Controller lấy user ID từ JWT và trả danh sách. Solution build Release đạt 0 warning, 0 error; người học báo đã test Swagger thủ công và các trường hợp danh sách, bộ lọc, rỗng, ngày sai và quyền đều đúng dự đoán. Bằng chứng này chưa phải automated API test. B2 chưa hoàn thành toàn bộ vì còn nối Web danh sách đơn, GET detail và báo sai; bước tiếp theo là nối màn “Đơn của tôi”.

`GET /api/order-menu?branchId={id}` đã được triển khai đủ DTO, `IOrderMenuRepository`/`OrderMenuRepository`, `IOrderMenuService`/`OrderMenuService`, DI và `OrderMenuController` với role `EMPLOYEE`. Build Release của Infrastructure/Application/API đạt 0 warning, 0 error. Bộ test hiện có 5/5 pass; ba test mới chứng minh Service dừng sớm khi branch không tồn tại, dừng trước khi đọc menu khi không có phân công, và ánh xạ đúng nhánh thành công gồm `MinPrice`, size, topping thường tối đa 3, up-size tối đa 1 và note tối đa một lựa chọn. Các unit test dùng mock nên không tự chứng minh EF/SQL hoặc HTTP status thật.

Ngày 2026-09-17, người học đăng nhập `employee01` và gọi Swagger vào SQL Server local với `branchId=2`; `GET /api/order-menu` trả HTTP 200 cho CN02. Agent parse toàn bộ response: 9 Category, 62 Product, 186 ProductSize, 3 ToppingGroup/10 Topping, 2 NoteGroup/6 NoteOption; không có Category/Product/Group rỗng, toàn bộ 62 `MinPrice` bằng giá ProductSize nhỏ nhất, topping thường đều 5K và `MaxQuantity=3`, `UP SIZE 1300ML` 10K và `MaxQuantity=1`. Bằng chứng này xác nhận Controller → Service → Repository → EF Core → `BranchFlowDB` local chạy thật cho happy path. Chưa kiểm chứng thủ công/HTTP các nhánh 400, sai role 403, sai phân công 403 và branch không tồn tại 404; đây là bước kế tiếp trước khi nối Web.

Người học sau đó báo đã kiểm tra Swagger và tất cả nhánh cho kết quả đúng dự đoán: `employee01` gọi CN01 đã hết phân công trả 403, branch không tồn tại trả 404, `branchId=0` trả 400, thiếu token trả 401 và token ADMIN bị `[Authorize(Roles = "EMPLOYEE")]` chặn 403. Như vậy lát BE đọc menu đã đạt build, unit test, EF/SQL local và kiểm tra HTTP thủ công; bước tiếp theo là nối Web màn EMPLOYEE 02. Việc tạo giỏ và `POST /api/orders` vẫn chưa bắt đầu.

Ngày 2026-09-17, Web màn EMPLOYEE 02 đã có `OrderMenu.razor`: gọi `UserApiService` để lấy `CurrentBranchId`, gọi `OrderMenuApiService`, xử lý loading/error/empty, hiển thị chi nhánh, Category và Product thật với `MinPrice` không có chữ “Từ”, ảnh thay thế khi thiếu `ImageUrl`, đồng thời lọc Product theo Category bằng `_selectedCategoryId` và `_visibleProducts`. Web build Release đạt 0 warning, 0 error. Người học báo đã test thủ công runtime: `Tất cả` hiện 62 món, chọn Category đổi nút active, số lượng và danh sách đúng, chọn lại `Tất cả` khôi phục 62 món. Tìm kiếm theo tên món bằng `@oninput` đã kết hợp với bộ lọc Category, có trạng thái không tìm thấy món và chuẩn hóa Unicode để bỏ dấu tiếng Việt; người học báo đã test thành công các trường hợp có dấu, không dấu, hoa/thường, kết hợp Category và xóa từ khóa. Đây là bằng chứng test thủ công do người học báo, chưa phải automated UI test. Phần danh sách/tìm kiếm màn EMPLOYEE 02 đã chốt. Vòng giao diện tĩnh của `Chọn món` đã hoàn thành đủ modal Bootstrap, backdrop, header, size, topping, hai nhóm ghi chú nhanh và số lượng món. Logic mở/đóng modal dùng duy nhất `_selectedProduct`: nút card truyền đúng DTO, pattern matching chỉ render modal/backdrop khi có món, nút đóng đặt lại null. Phần chọn size đã dùng danh sách `selectedProduct.ProductSizes`, giữ một `_selectedProductSize`, đánh dấu đúng một nút active, hiển thị tên/giá thật và chỉ mở footer sau khi chọn. Phần topping đã render `ToppingGroups` và `Toppings` thật, dùng Dictionary theo `Topping.Id`, khóa nút ở 0/`MaxQuantity`, giới hạn topping thường ở 3 và up-size ở 1, đồng thời reset khi đổi hoặc đóng món. Phần ghi chú đã render `NoteGroups`/`NoteOptions` thật, dùng Dictionary theo `NoteGroup.Id` để giữ tối đa một lựa chọn trong mỗi nhóm, cho phép bấm lại để bỏ chọn và reset khi đổi hoặc đóng món. Phần số lượng món đã dùng `_productQuantity`, giới hạn tối thiểu `1`, khóa nút giảm tại `1`, reset khi mở/đóng món và dựa vào cơ chế render lại sau event của Blazor. Phần tạm tính đã dùng công thức `(ProductSize.Price + tổng Topping.UnitPrice × topping quantity) × product quantity`, hiển thị định dạng `N0` ở footer và không thay đổi theo ghi chú. Giỏ tạm đã có ba Web model cho item/topping/note, sao chép cấu hình đã chọn vào `_cartItems`, giữ ID và quantity để chuẩn bị request nhưng chỉ dùng giá/tên hiện tại để hiển thị. Badge giỏ cộng `OrderCartItemModel.Quantity`, không dùng số dòng. Modal giỏ có state `_isCartOpen` độc lập với dữ liệu, mở được khi rỗng, đóng không xóa món và có nội dung cuộn trên mobile. Chi tiết giỏ đã render mọi dòng với tên món, size, quantity, giá size, tạm tính, topping và ghi chú; bỏ các khu vực tùy chọn khi rỗng. Tổng tiền hiển thị của giỏ cộng các `DisplaySubtotal`, dùng `decimal`, đóng modal không xóa dữ liệu. Mỗi dòng có thể được xóa bằng chính `OrderCartItemModel` đang được `foreach` render, nên hai cấu hình dùng cùng `ProductSizeId` vẫn được phân biệt. Ngày 2026-09-19 người học báo đã test giỏ rỗng, nhiều dòng, đóng/mở, thêm tiếp món, xóa đúng một cấu hình, cập nhật badge/tổng tiền và xóa dòng cuối vẫn hiện trạng thái rỗng đúng dự kiến. Agent build Web Release đạt 0 warning, 0 error. Đây là test thủ công runtime, chưa phải automated UI test. Ba request DTO và `CreateOrderResponseDTO` của `POST /api/orders` đã được tạo và build toàn solution Release đạt 0 warning, 0 error: request chỉ gửi `BranchId`, từng `ProductSizeId`/quantity, topping ID/quantity và note option ID; không gửi tên hoặc giá hiển thị. Response có mã đơn, branch, ngày nghiệp vụ, tổng tiền Backend tính, trạng thái và thời điểm tạo. Endpoint chưa triển khai. Bước tiếp theo là result DTO để Service truyền kết quả nghiệp vụ cho Controller.

Thiết kế B2 đã chốt bước đầu cho `GET /api/order-menu?branchId`: màn EMPLOYEE 02 hiển thị giá của `ProductSize` hoạt động có giá nhỏ nhất, nhưng UI chỉ hiện số tiền và **không có chữ “Từ”**. API trả `MinPrice` và danh sách `ProductSizes` của từng món (`Id`, `SizeId`, `SizeName`, `Price`) để khi bấm món mở chọn size mà không gọi endpoint khác; màn 02 chưa hiển thị danh sách này. `ToppingGroups` và `NoteGroups` được trả một lần ở cấp cao nhất của response, không lặp trong 62 Product; Web chỉ dùng chúng khi mở tùy chọn món. Endpoint dùng `[Authorize(Roles = "EMPLOYEE")]`: branch không tồn tại/đã xóa trả 404; branch tồn tại nhưng employee không có `UserBranch` còn hiệu lực trả 403; ADMIN/OWNER dùng endpoint quản lý khác. Branch hợp lệ và có quyền nhưng không còn món bán vẫn trả 200 với các danh sách rỗng; Web hiển thị trạng thái không có món, không dùng 404. Sau lọc, API bỏ Category không có Product, Product không có ProductSize, ToppingGroup không có Topping bán và NoteGroup không có NoteOption hoạt động. Response có `branch` gồm `Id`, `Code`, `Name`; không trả địa chỉ hoặc dữ liệu quản lý không dùng. Chưa khóa field chi tiết topping/note DTO.

Ảnh hậu kiểm ngày 2026-09-16 xác nhận `002-seed-tuan-kiet-menu.sql` đã chạy thành công trên Azure SQL: Category 9, Product 62, ActiveSize 3, ProductSize 186, Topping 10, BranchProduct 62 và BranchTopping 10. Lần chạy local đầu tiên dừng an toàn vì script cũ tìm code DEMO; sau khi đổi sang mọi branch hoạt động, ảnh hậu kiểm `BranchFlowDB` local xác nhận Category 9, Product 62, ActiveSize 3, ProductSize 186, Topping 10, BranchProduct 124 và BranchTopping 20 cho hai branch CN01/CN02. Các số lượng khớp hoàn toàn với điều kiện script, nên B1 hoàn thành ở Azure và local. Chưa có bằng chứng API đọc menu vì endpoint chưa được viết; bước học chuyển sang B2 tạo DTO rồi thiết kế `GET /api/order-menu?branchId` cho EMPLOYEE.

Đã review file ngoài repo `Seed_TuanKiet_Menu(2).sql`: bản gốc có 49 dòng và dùng đúng cấu trúc Category/Product/ProductSize, nhưng sai database, thiếu BranchProduct, có xung đột cách gọi size M/L với dung tích và có nguy cơ re-run ghi đè giá. Người học xác nhận mỗi phần ngăn bởi `/` là một món riêng. Đã tạo và chạy `database/azure/002-seed-tuan-kiet-menu.sql`: tách thành 62 Product duy nhất, 3 ProductSize mỗi món (186 dòng), dùng 500ML/700ML/1000ML, ngừng M/L cũ nếu chưa được dùng và liên kết 62 món với branch DEMO. Theo ảnh menu topping, script có 9 topping thường đơn giá 5K/phần (quantity 1/2/3 thành 5K/10K/15K) cùng `UP SIZE 1300ML` là topping đặc biệt 10K, rồi tạo 10 BranchTopping cho DEMO. Service sau này phải giới hạn topping thường quantity 1..3 và up size đúng 1; schema hiện chỉ check Quantity > 0. Script kiểm tra DB/branch/số dòng, chặn chạy lại khi Product đã có và hiện cho phép chạy trên `BranchFlowDB` local hoặc `BranchFlowDB-Demo` Azure.

Ảnh kiểm kê Azure SQL ngày 2026-09-16 xác nhận đang ở `BranchFlowDB-Demo`; có một branch `DEMO` (Id 1, hoạt động, chưa xóa). Các số đếm nhìn thấy: Branch 1, Category 0, Product 0, Size 2, ProductSize 0, ToppingGroup 3, Topping 0, NoteGroup 2. Vì vậy DB cloud chưa có dữ liệu món để phục vụ `GET /api/order-menu`; cần tạo script dữ liệu menu ban đầu từ menu/giá thật của quán. Số lượng NoteOption, BranchProduct và BranchTopping chưa nhìn thấy trong ảnh nên chưa kết luận; không tự bịa dữ liệu menu.

Người học đã chốt **một menu và giá chung cho mọi chi nhánh**, product/topping availability theo branch, và **chỉ một tài khoản ADMIN duy nhất xuyên suốt dự án hiện tại**. ADMIN này được sửa menu chung; thao tác riêng branch vẫn kiểm tra phân công còn hiệu lực. Không thêm cấp ADMIN/cờ quyền hoặc ràng buộc singleton DB khi chưa có yêu cầu triển khai cụ thể; phân cấp/nhiều ADMIN sẽ cập nhật nghiệp vụ sau dự án. Quán có menu ngoài thực tế nên dữ liệu đầu tiên có thể nhập bằng script chạy một lần; trang Menu ADMIN dùng để vận hành thay đổi về sau. Đã cập nhật [API roadmap v0.3](../context/api-endpoint-roadmap-v0.3.md): seed/kiểm tra dữ liệu ban đầu → gọi món và tạo order EMPLOYEE → CRUD Menu ADMIN → order ADMIN/OWNER → hồ sơ EMPLOYEE → kho → phần còn lại. Roadmap v0.1/v0.2 giữ lịch sử. Route mới chưa được code/khóa contract; không suy DB public thiếu món từ việc repo thiếu seed. Mỗi mốc vẫn làm từng lát BE + FE + test.

Người học chỉ ra prototype `EMPLOYEE/02_mobile_goi-mon.png` là màn gọi món đầu tiên, không hiển thị topping/note. Agent đã xem ảnh 02–04: màn 02 hiển thị chip Category, thẻ Product (tên/ảnh) và giá bắt đầu "Từ ..."; màn 03 mới mở chọn size/topping; màn 04 mới có note và số lượng. Trước đó hướng dẫn đã gộp dữ liệu toàn luồng chọn món vào màn đầu mà chưa nêu phạm vi, nên đã sửa điểm dừng học: hiện chỉ chốt dữ liệu cho màn 02; chưa quyết định GET menu trả toàn bộ lựa chọn cùng lúc hay tải khi mở tùy chọn. Khối "Món thường gọi 30 ngày gần nhất" còn phụ thuộc lịch sử order, không suy từ menu tĩnh.

Checkpoint menu sản phẩm: người học đã phân biệt `Size.Id` (loại size dùng chung) với `ProductSize.Id` (cặp món–size và giá); trả lời đúng giỏ phải giữ `ProductSize.Id` khi chọn một món-size cụ thể. Cấu trúc tối thiểu đang phác thảo là Category Id/Name → Product Id/Name/ImageUrl → ProductSize Id/SizeName/Price; đây chưa phải DTO/code đã triển khai.

Người học giải thích đúng khi tạo order API phải đọc lại giá hiện tại từ database thay vì tin giá Web, vì menu có thể thay đổi và request Web có thể bị sửa. Chưa thiết kế/triển khai `POST /api/orders`; quay lại chốt DTO đọc menu.

Người học chọn đúng `ProductSize.Id` thay vì `Product.Id` cho món đã chọn size, vì giá nằm tại `ProductSize` liên kết với `Size`. Chưa chốt toàn bộ output contract của GET menu.

Sau checkpoint deploy thử, người học đã thêm unit test cho `EmployeeService.GetEmployeesByBranchAsync()` với role `ADMIN`, chi nhánh tồn tại nhưng `HasActiveAssignmentAsync()` trả `false`; kết quả `IsBranchFound = true`, `HasAccess = false` và không gọi repository lấy nhân viên. Agent chạy `dotnet test` Release: 2 Passed, 0 Failed. Người học đã giải thích đúng Controller gọi Service, Service kiểm tra phân công rồi dừng trước `EmployeeRepository.GetByBranchAsync()` khi không có quyền; Controller ánh xạ từ chối thành 403. Đã phân biệt test hiện có chứng minh nhánh Service, chưa kiểm chứng HTTP 403 của Controller hoặc database thật. Checkpoint unit test đã đạt; đang thiết kế từng bước `GET /api/order-menu?branchId={id}` cho employee xem menu bán theo chi nhánh, chưa code endpoint. Người học nêu đúng employee, kiểm tra phân công/availability theo branch, giá món theo `ProductSize` và giá topping theo `Topping`. Sau khi được giải thích số lượng nằm trong giỏ Web, người học trả lời đúng giỏ được gửi bằng `POST /api/orders` khi xác nhận; không gửi số lượng qua GET menu. Category, size, topping/note group cần được chốt trong DTO menu, chưa có output contract hoàn chỉnh.

Cập nhật mốc 2026-09-15: Azure SQL demo đã có 30 bảng, dữ liệu demo và tài khoản SQL runtime quyền hạn chế. API đã deploy thủ công lên Linux App Service .NET 10 ở East Asia; `/health` public qua HTTPS trả 200 Healthy. Người học báo login `admin_demo` trên API cloud trả 200 và nhận access token sau khi sửa connection string; `/api/auth/me` với Bearer token trả 200, username `admin_demo`, role `ADMIN`. `GET /api/branches/accessible` với Bearer token trả 200 và chi nhánh DEMO (Id 1); `GET /api/employees?branchId=1` trả 200, nhân viên demo `NV_CLOUD_01` (Id 1). Web publish chạy Production ở `localhost:5032` với `ApiSettings__BaseUrl` trỏ API cloud; ảnh người học gửi cho thấy đã đăng nhập `admin_demo` và danh sách nhân viên hiện đúng `NV_CLOUD_01` ở chi nhánh demo. SSMS cũng kết nối lại thành công sau lỗi pre-login timeout tạm thời. Chưa deploy Web cloud hoặc chứng minh luồng UI public. Các mô tả bước cũ bên dưới là lịch sử.

CI restore/build/test và luồng API/Web Production local đã được người học báo kiểm chứng. Người học giải thích đúng: tắt Web local thì `localhost:5032` không truy cập được, API cloud vẫn hoạt động độc lập. Azure CLI trước khi tạo Web xác nhận `asp-branchflow-demo` ở East Asia, Basic B1 và chỉ `branchflow-api-khang153` Running. Ảnh wizard Basics/Database/Deployment/Networking/Monitor và Review xác nhận Web mới `branchflow-web-khang153` dùng Azure for Students, `rg-branchflow-demo`, Code .NET 10 Linux East Asia, **existing** `asp-branchflow-demo (B1)`, không tạo DB, không bật CD/basic authentication, public access On, VNet Off, Application Insights No và Defender bỏ chọn. Ảnh Overview sau Create cho thấy `branchflow-web-khang153` **Running**, plan `asp-branchflow-demo (B1:1)`, Linux East Asia, Application Insights chưa bật và deployment provider None. Người học báo `dotnet publish` Web Release thành công (Build succeeded in 3.4s); agent thấy `TuanKietBranchFlow.Web.dll` và `.runtimeconfig.json` ở `artifacts/web-linux`. ZIP `artifacts/branchflow-web-linux.zip` có entry `./` và hai file runtime ở gốc, không có thư mục bao ngoài. Agent đọc Azure CLI thấy App setting `ApiSettings__BaseUrl` của Web trỏ HTTPS API cloud đúng hostname và có `/` cuối URL. Người học đã bắt đầu `az webapp deploy`: upload/build thành công nhưng CLI còn lặp "Starting the site" sau 268 giây, chưa có kết quả cuối. Agent kiểm tra Azure troubleshoot: instance `Started`, không có startup failure; HTTPS Web root trả 200 HTML tiếng Việt của ứng dụng. Ảnh người học gửi cho thấy Web cloud render `MainLayout` với Home, Chi nhánh và nút Đăng nhập trên hostname Azure HTTPS. `Hello, world!` là markup còn nguyên trong `Home.razor`, không phải lỗi deploy. Người học báo sau thao tác login trên Web cloud, layout hiện `Xin chào, admin_demo`; đây là bằng chứng test thủ công của login/Interactive Server, chưa có ảnh, role hoặc kết quả danh sách nhân viên public.

Lệnh `az webapp deploy` cuối cùng trả `RuntimeFailed` với lý do worker không start trong 10 phút, không thể ghi là deploy CLI thành công. Tuy nhiên agent đọc Kudu cùng deployment ID `dcf8af1e-2f7f-4236-aa7e-3cba92f77893`: build và OneDeploy ghi `Deployment successful`; startup log cùng instance ghi chạy `TuanKietBranchFlow.Web.dll` trên cổng 8080, Production và warmup probe thành công sau khoảng 31 giây. Site status vẫn `Started` không lỗi, HTTPS `/login` trả 200 có form; người học trước đó đã báo login hiện `admin_demo`. Sau thông báo timeout, người học báo đã test Web cloud và các chức năng chạy như local; tiếp tục báo **điện thoại và trình duyệt khác đều hoạt động tốt**. Đây là smoke test thủ công của URL public/vertical slice từ thiết bị khác, không phụ thuộc browser localhost, nhưng chưa có kết quả từng case hoặc automated regression test; chưa xác nhận đã tắt Web local. M2 đạt mốc chức năng public theo bằng chứng người học báo, không đồng nghĩa production hoàn chỉnh. DeploymentStatus và runtime evidence mâu thuẫn; chưa xác định nguyên nhân lỗi status, không tự gọi quy trình deploy là thành công sạch hoặc chuyển sang CD.

Ảnh portal `Deployment Failure Analysis Linux` tại 2026-09-15 09:55 UTC/24h cho thấy **No Configuration Checks issues**, **No Uploaded Package Validations issues**, **No Package Mounting Checks issues**. Detector không đưa nguyên nhân cụ thể cho deploymentStatus `RuntimeFailed`; ba check xanh không phủ định lỗi trạng thái. Báo cáo cũng ghi phạm vi hỗ trợ detector còn giới hạn theo SKU. Ảnh `Container Issues` trong cùng 24h ghi **Your app started successfully** và **No app start failures were found**. Kết hợp Kudu thành công, startup probe thành công và smoke test public trên thiết bị khác, Web hiện hoạt động; nguyên nhân chính xác của deploymentStatus `RuntimeFailed` vẫn chưa được xác định. Không đổi cấu hình, tăng timeout, scale hoặc redeploy chỉ để xóa trạng thái đỏ; cần kiểm chứng lại ở lần phát hành thủ công kế tiếp trước khi tự động hóa CD.

## Mốc vừa hoàn thành

- Đã tạo `TuanKietBranchFlow.Tests` dùng xUnit và Moq, target `net10.0`, tham chiếu Application và được thêm vào solution. Bài test gọi `EmployeeService.CreateEmployeeAsync()` thật, kiểm tra ngày tương lai bị từ chối, không trả Employee và không gọi repository/hasher/lưu. Agent build Release: 0 warning, 0 error; test: 1 Passed, 0 Failed. Người học báo bước Test trên GitHub thành công; chưa phải kiểm thử toàn bộ nghiệp vụ.

- Đã có các API nhân viên: danh sách, chi tiết, tạo, cập nhật, chuyển chi nhánh và đổi trạng thái.
- `PATCH /api/employees/{employeeId}/status?branchId=...` cập nhật thống nhất `AppUser`, `EmployeeProfile` và `UserBranch`, sau đó lưu một lần qua Unit of Work.
- Khi cho nghỉ, `LeaveDate` là ngày bắt đầu nghỉ và `ActiveTo = LeaveDate - 1 ngày`.
- Service đã chặn ngày nghỉ không hợp lệ để tránh vi phạm ràng buộc `ActiveFrom <= ActiveTo` trong database.
- Truy vấn danh sách và chi tiết đều xem được nhân viên đang làm hoặc nhân viên đã nghỉ tại đúng chi nhánh phát sinh việc nghỉ.
- Agent đã build solution bằng thư mục output tạm: 0 warning, 0 error. Build thông thường từng bị khóa DLL do API đang chạy; đó không phải lỗi code.
- Người học báo đã test thủ công bằng Swagger và mọi kết quả đúng dự đoán, gồm danh sách, chi tiết, đổi trạng thái và phạm vi chi nhánh. Đây chưa phải automated test hoặc kiểm thử production.
- `Login.razor` đã gọi `AuthApiService`, xử lý validation/loading/thông báo và build Web đạt 0 warning, 0 error.
- Đã cài `Blazor.LocalStorage 10.1.0`, đăng ký `ILocalStorageService` và lưu access token với key `accessToken` sau khi đăng nhập thành công.
- Người học báo đã test trên trình duyệt: đăng nhập thành công, token xuất hiện trong LocalStorage và vẫn còn sau F5. Đây là test thủ công, chưa chứng minh token đã được gắn vào request API khác.
- `AuthApiService.GetCurrentUserAsync()` đã gắn `Authorization: Bearer`, gọi `GET /api/auth/me` và build Web đạt 0 warning, 0 error.
- Người học báo đã test runtime thành công: `Login.razor` đọc lại token, `/api/auth/me` trả `CurrentUserDTO`, giao diện hiển thị đúng username và role. Đây là test thủ công trên local, chưa phải automated test.
- Đã tạo `CustomAuthenticationStateProvider`, đăng ký cùng một scoped instance cho kiểu cụ thể và `AuthenticationStateProvider`, đồng thời chuyển `Routes` sang Interactive Server toàn cục.
- `Login.razor` gọi `MarkUserAsAuthenticated()` sau khi `/api/auth/me` thành công; `MainLayout` dùng `AuthorizeView` để hiện username hoặc nút đăng nhập.
- Web đã build sau thay đổi authentication state: 0 warning, 0 error. Người học báo đã test thủ công: sau login layout hiện username; sau F5 layout trở về anonymous trong khi token vẫn còn LocalStorage, đúng với trạng thái hiện tại chưa có restore.
- `MainLayout.OnAfterRenderAsync(firstRender)` đã gọi provider đọc token từ LocalStorage, gọi `/api/auth/me` và tái tạo `ClaimsPrincipal` khi token hợp lệ.
- Web build sau phần restore đạt 0 warning, 0 error. Người học báo đã test thủ công đủ ba trường hợp: token hợp lệ giữ username sau F5; không có token hiện nút đăng nhập; token giả bị API từ chối, bị xóa và giao diện trở về anonymous.
- Provider đã có `LogoutAsync()` để xóa access token, tạo `ClaimsPrincipal` anonymous và thông báo lại authentication state. `MainLayout` hiện username/nút logout khi authorized và nút login khi anonymous.
- Web build sau phần logout đạt 0 warning, 0 error. Người học báo đã test thủ công: logout chuyển về `/login`, token bị xóa khỏi LocalStorage và F5 vẫn giữ trạng thái anonymous.
- Thử nghiệm `ApiAuthorizationHandler` build xanh nhưng browser test tái hiện lỗi runtime: handler do `IHttpClientFactory` tạo trong scope riêng, nên `ILocalStorageService` không dùng được JavaScript runtime của circuit và ném `InvalidOperationException`. API đo trực tiếp vẫn nhanh (`/login` khoảng 48 ms và `/me` khoảng 3 ms sau warm-up); cảm giác login lâu là do circuit chờ rồi bị ngắt, không phải SQL/JWT chậm.
- Đã thay handler bằng `AuthorizedApiService` chạy trong Blazor circuit, gỡ `ApiAuthorizationHandler` và client pipeline lỗi. Web build đạt 0 warning, 0 error.
- Agent đã chạy browser test thật bằng Playwright trên bản build mới: login hợp lệ chuyển về trang chủ trong khoảng 292 ms, F5 vẫn hiện `admin01`, console không có lỗi và logout xóa sạch LocalStorage. Đây là test tự động hóa local cho flow UI, chưa phải test source được commit vào repository.
- Người học đã restart Web chính ở cổng `5032` và báo test thủ công đúng dự đoán: login nhanh, restore sau F5 và logout đều hoạt động với cấu hình DI mới.
- Trang danh sách nhân viên đã nhận `branchId`, tìm theo từ khóa và lọc đủ ba trạng thái. Người học báo đã test thủ công thành công sau khi giao diện dùng chuỗi cho giá trị `<select>` rồi chuyển thành `bool?` trước khi gọi API.
- Trang hồ sơ nhân viên đã đi hết flow từ nút Hồ sơ ở danh sách đến `GET /api/employees/{employeeId}?branchId=...`. Agent đã kiểm tra trên trình duyệt local: hồ sơ nhân viên nghỉ hiển thị đầy đủ thông tin, ngày nghỉ và lịch sử chi nhánh; Web build đạt 0 warning, 0 error.
- Người học báo đã test thủ công thêm hồ sơ nhân viên đang làm và ID không tồn tại, kết quả đúng dự đoán. Vertical slice đọc nhân viên đã hoàn thành ở mức local/manual, chưa có automated test lưu trong source.
- API tạo nhân viên đã chặn `HireDate` trong tương lai tại `EmployeeService` và ánh xạ thành HTTP 400 tại `EmployeesController`; solution build 0 warning, 0 error. Người học báo đã test thủ công Swagger: ngày tương lai trả 400, ngày hợp lệ tạo thành công.
- `EmployeeCreate.razor` đã dùng chuỗi `dd/MM/yyyy`, `DateOnly.TryParseExact` và kiểm tra ngày tương lai trước khi gọi API; Web build 0 warning, 0 error. Người học báo đã test thủ công đủ ngày hợp lệ, sai định dạng, ngày không tồn tại, ngày tương lai và sửa lại sau lỗi, kết quả đúng dự đoán.
- Web đã có `EmployeeCreateApiResult`; `EmployeeApiService` đọc `ProblemDetails`, status code và dữ liệu thành công thay vì đổi mọi lỗi thành `null`. Web build 0 warning, 0 error. Người học báo đã test thủ công các lỗi 401/403/404/409 và trường hợp tạo thành công, kết quả đúng dự đoán.
- Commit `a64f184` đã thêm `.github/workflows/ci.yml`. Ảnh GitHub Actions do người học cung cấp cho thấy lần chạy đầu tiên của `BranchFlow CI` đã hoàn thành màu xanh: checkout source, cài .NET SDK, restore NuGet và build solution ở cấu hình Release đều thành công. Đây là CI build thật trên runner độc lập, chưa có automated test, chạy ứng dụng hoặc deploy.
- API và Web đã được publish local vào `artifacts/`. Ảnh terminal do người học cung cấp cho thấy bản `TuanKietBranchFlow.Api.dll` khởi động thành công ngoài Visual Studio tại `http://localhost:5107`, môi trường `Production`, với content root đúng là `artifacts/api`. Đây mới chứng minh startup và Kestrel lắng nghe; chưa chứng minh request login, kết nối database hoặc toàn bộ vertical slice runtime.

## Kiến thức người học đã trình bày đạt

- Người học giải thích đúng không cần đổi schema hoặc tạo ba bản ghi giá cho cùng topping vì `OrderItemTopping` có `Quantity × UnitPriceSnapshot`; topping thường dùng đơn giá 5K với quantity 1..3. Service phải giới hạn topping thường tối đa 3 và up-size đúng 1 vì constraint DB hiện chỉ bảo đảm `Quantity > 0`.
- Người học báo smoke test API publish: login trả 200 và có token; `/api/auth/me` trả 200 với username/role đúng. Sau khi bật SQL Docker, request login hoạt động; đây là test thủ công local, không phải agent trực tiếp chạy test.
- Người học báo đã chạy Web publish Production tại cổng 5207, gọi API publish tại 5107, test toàn bộ chức năng hiện có trên giao diện và không thấy lỗi log. Đây là bằng chứng người học báo test thủ công vertical slice local, chưa phải deploy public hoặc automated regression test.
- Đã hiểu Content Root quyết định nơi đọc cấu hình, đăng ký DbContext chưa mở kết nối SQL, và login tạo token khác với middleware xác thực Bearer token.

- `PATCH` phù hợp vì endpoint chỉ đổi trạng thái, không cập nhật toàn bộ hồ sơ.
- Nghỉ việc không phải soft delete: vẫn giữ hồ sơ và lịch sử phân công.
- Một phân công kết thúc chưa đủ kết luận nhân viên nghỉ; phải kết hợp `AppUser.IsActive`, `EmployeeProfile.LeaveDate` và `UserBranch.ActiveTo`.
- `Include/ThenInclude` tải dữ liệu liên quan để ánh xạ DTO; `Any` dùng làm điều kiện lọc trong truy vấn SQL.
- Lỗi dữ liệu nghiệp vụ nên được kiểm tra để trả 400/409 phù hợp, không chờ database ném lỗi thành 500.
- Flow đã hiểu: JWT middleware -> Controller -> Service -> Repository/Unit of Work -> DbContext -> SQL -> DTO -> HTTP response.
- Đã phân biệt trigger `on.push.branches` với `actions/checkout`: trigger quyết định lúc workflow chạy, còn checkout tải source của commit tương ứng xuống runner.
- Đã phân biệt CI build xanh với runtime/deploy/production: build xanh chỉ chứng minh source đã commit có thể checkout, restore và compile Release trên runner độc lập.
- Đã phân biệt `RuntimeFailed` là trạng thái thất bại của lần deploy Web, không phải bằng chứng Web hiện đang sập: người học nêu đúng startup log, HTTPS và thử trên điện thoại cho thấy Web hiện hoạt động. Người học cũng nêu đúng chưa biết nguyên nhân mâu thuẫn và chưa coi đường deploy là xanh để làm CD.

## Quyết định nghiệp vụ đang giữ

- `EffectiveDate` khi nghỉ là ngày đầu tiên nhân viên nghỉ.
- Khi nghỉ: `EmployeeProfile.LeaveDate = EffectiveDate` và phân công hiện tại có `ActiveTo = EffectiveDate.AddDays(-1)`.
- Ngày nghỉ sớm nhất phải sau `ActiveFrom`; nếu bắt đầu làm ngày 10/09 thì làm hết ngày 10/09 và sớm nhất nghỉ từ 11/09.
- Khi hoạt động lại: xóa `LeaveDate`, bật `AppUser.IsActive` và tạo một `UserBranch` mới; không sửa mất lịch sử cũ.
- ADMIN chỉ thao tác trong chi nhánh còn được phân công; nhân viên nghỉ được xem tại chi nhánh nơi phát sinh lần nghỉ đó.

## Bước kế tiếp duy nhất

Thiết kế contract đọc đầu tiên cho `GET /api/order-menu?branchId=1`: xác định authorization/branch scope và DTO tối thiểu cho màn EMPLOYEE 02 trước, sau đó mới quyết định có trả luôn topping/note cho màn 03–04 hay tải khi mở món. Không viết `POST /api/orders` trước khi endpoint đọc menu, test quyền và UI màn 02 hoạt động.

## Việc cần trước khi public production

- JWT đã cấp chưa được chứng minh bị vô hiệu ngay khi `AppUser.IsActive` chuyển thành false; cần chốt chiến lược kiểm tra hoặc thu hồi token.
- Chưa có automated test cho các luật ngày hiệu lực và phạm vi chi nhánh.
- CI đã có restore/build Release/test; người học báo chạy thành công trên GitHub. Hiện mới có một bài unit test, chưa có integration test API/SQL.
- Web đã có authentication state, khôi phục sau F5, logout và `AuthorizedApiService` gắn Bearer token trong đúng circuit. Chưa có xử lý lỗi tập trung khi API/Web mất kết nối và chưa có automated test được lưu trong source.
- API cloud đã có xử lý lỗi production, health check và các bài smoke test tích cực cho login, Bearer/role/chi nhánh DEMO, danh sách nhân viên; còn cần kiểm chứng các trường hợp bị từ chối quyền, Web public, vận hành ổn định và các kiểm tra bảo mật trước khi gọi là production hoàn chỉnh.
