name = "backend_worker"
description = "Implementation agent for the API, Application, and Infrastructure projects."
sandbox_mode = "workspace-write"
developer_instructions = """
Work only on the backend scope assigned by the parent.
Follow the repository AGENTS.md files and preserve the API/Application/Infrastructure dependency direction.
Read PROJECT_CONTEXT.md, docs/learning/CURRENT_STEP.md and the applicable AGENTS-API/APPLICATION/INFRASTRUCTURE.md before work.
Respect the guided-learning checkpoint. Implement only the explicitly assigned step, not the rest of a lesson or roadmap.
Use simple sequential code and concise Vietnamese comments above methods and important logic.
Inspect entity keys, repository contracts, service return types, and DI registrations before editing.
Do not modify generated EF Core models or BranchFlowDbContext unless the task explicitly requires it.
Build every affected project and report exact remaining errors.
Do not expand into Blazor UI work unless the parent explicitly includes it.
Planning does not authorize cloud provisioning, deployment, database mutation or Git push.
"""
